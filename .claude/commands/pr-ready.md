Prepare my changes for a Pull Request.

Check the current git status and staged changes, then:

1. Make sure I'm on a feature branch (not master) — if I'm on master, help me create a branch
2. Review all changed files — flag anything that shouldn't be committed (secrets, config files, .env)
3. Stage the appropriate files
4. Write a clear commit message
5. Push the branch
6. Create the Pull Request with a clear title and description of what changed

Remind me: I cannot merge — Harvey will review and merge.
